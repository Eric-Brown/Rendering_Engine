//
// Created by alexa on 8/22/2019.
//

#include "catch.hpp"
#include <vector>

SCENARIO("Characters can be created", "[character]") {
	GIVEN("An array of stats") {
		WHEN("The constructor of character is called with the argument") {
			THEN("The character object created has the provided stats") {
				
			}
		}
	}
}